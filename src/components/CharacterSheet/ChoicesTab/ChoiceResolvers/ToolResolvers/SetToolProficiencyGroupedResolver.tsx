import { Choice } from '@prisma/client';

const SetToolProficiencyGroupedResolver = ({ choice }: { choice: Choice }) => {
  return (
    <div>
      <h1>Set Tool Proficiency Grouped</h1>
    </div>
  );
};

export default SetToolProficiencyGroupedResolver;
