import { Choice } from '@prisma/client';

const SetToolProficiencyResolver = ({ choice }: { choice: Choice }) => {
  return (
    <div>
      <h1>Set Tool Proficiency</h1>
    </div>
  );
};

export default SetToolProficiencyResolver;
