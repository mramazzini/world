import { Choice } from '@prisma/client';

const SetAbilityScoreResolver = ({ choice }: { choice: Choice }) => {
  return (
    <div>
      <h1>Set Ability Score</h1>
    </div>
  );
};

export default SetAbilityScoreResolver;
