import { Choice } from '@prisma/client';

const SetSavingThrowProficiency = ({ choice }: { choice: Choice }) => {
  return (
    <div>
      <h1>Set Saving Throw Proficiency</h1>
    </div>
  );
};

export default SetSavingThrowProficiency;
