import { Choice } from '@prisma/client';

const SetWeaponProficiencyResolver = ({ choice }: { choice: Choice }) => {
  return (
    <div>
      <h1>Set Weapon Proficiency</h1>
    </div>
  );
};

export default SetWeaponProficiencyResolver;
